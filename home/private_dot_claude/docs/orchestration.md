Moved. This guidance now lives in the `orchestrating-subagents` skill
(`~/.claude/skills/orchestrating-subagents/SKILL.md`), so it loads when you're
actually about to fan out rather than in every session's context window.
