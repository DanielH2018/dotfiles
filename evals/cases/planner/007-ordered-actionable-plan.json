{
  "id": "planner/007-ordered-actionable-plan",
  "agent": "planner",
  "description": "The plan must be an ordered, executable sequence, not an unordered list of considerations",
  "input": "Plan the migration of our web session store from in-process memory to Redis.",
  "assert": { "must_match": [], "must_not_match": [] },
  "rubric": "PASS only if the plan includes an ordered, actionable sequence of steps (the order/dependencies are clear enough that the implementer could execute them in turn), rather than only an unordered list of considerations or risks.",
  "k": 5,
  "threshold": "rate>=4/5"
}
