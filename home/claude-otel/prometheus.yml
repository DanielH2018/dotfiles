global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Scrape the metrics the OTEL collector re-exposes from Claude Code's OTLP stream.
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
