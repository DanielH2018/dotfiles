# Exclude persistent data and non-build files from build context.
# Only claude.json, entrypoint.sh, and vault-search are referenced by COPY
# during build. Everything else is runtime-only (bind-mounted by claude-sandbox).
state/
audit/
sessions/
artifacts/
repo-snapshots/
dockerfiles/
claude-sandbox
_claude-sandbox
settings.json
audit.sh
*.md
