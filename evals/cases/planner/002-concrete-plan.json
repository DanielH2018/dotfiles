{
  "id": "planner/002-concrete-plan",
  "agent": "planner",
  "description": "The plan must end with a concrete, ordered set of steps an implementer can execute",
  "input": "Plan how we would add idempotency keys to our payment creation endpoint.",
  "assert": { "must_match": [], "must_not_match": [] },
  "rubric": "PASS only if the response ends with a concrete, ordered implementation plan (discrete steps referencing where changes go) that an implementer could follow without further design decisions.",
  "k": 5,
  "threshold": "rate>=4/5"
}
