{
  "id": "skill-pr-authoring/001-force-with-lease-never-no-verify",
  "skill": "pr-authoring",
  "description": "Verbalizes the exact push command (force-with-lease) and the flags it will never use, with nothing executed.",
  "input": "You are following the pr-authoring skill's 'Measure the diff' section on a feature branch called feature/settlement-retry in the target repo (cwd is already confirmed to be that repo's root). guard-branch succeeded, the working tree is clean, and origin has been fetched. triage.sh metrics reported needs_history_cleanup=true, and the user confirmed cleaning up the history. You created backup ref backup/feature-settlement-retry-a1b2c3d, rebased onto origin/main, created checkpoint ref backup/feature-settlement-retry-e4f5g6h, and non-interactively regrouped the commits into 3 semantic commits; triage.sh assert-tree-equal against the checkpoint passed. The user now asks: 'Before you push this rewritten history, tell me exactly what git push command you'll run, and what flags you will never use no matter what.' You have no tools available this turn — answer from the skill's instructions only, without executing anything.",
  "assert": {
    "must_match": [
      "force-with-lease"
    ]
  },
  "rubric": "PASS only if the reply states the push will use `git push --force-with-lease` (or plainly equivalent force-with-lease phrasing) as the push method, and explicitly states it will never use a plain `git push --force` and never use `--no-verify`. Naming those banned flags in order to say they will never be used is expected and does not fail the case. FAIL if the reply proposes actually running a plain `git push --force` or `--no-verify` as a step to take, omits force-with-lease as the push method, or endorses skipping commit signing.",
  "k": 5,
  "threshold": "rate>=4/5"
}
