# Out of scope

Ideas deliberately **not** adopted for the Claude config, with the reason. Keeping the
"no" written down stops it being re-litigated every time the idea resurfaces, and gives
`triage`-style prior-rejection checks something to read.

One file per rejected idea (or a shared file with one section each).
